**Root cause:** `grep -i "openclaw-gateway"` doesn't match the actual command line, which contains `openclaw/dist/index.js gateway` (no hyphen, different substring).

**Fix:**
```bash
CURRENT_PID=$(pgrep -f "openclaw/dist/index.js gateway" | head -1)
```

**Verify:**
```bash
pgrep -f "openclaw/dist/index.js gateway" && echo OK || echo MISS
```